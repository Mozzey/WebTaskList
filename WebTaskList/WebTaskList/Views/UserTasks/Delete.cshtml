@model WebTaskList.Domain.Models.UserTask

@{
    ViewBag.Title = "Delete";
}
<div class="container">
    <div class="row" style="margin-top: 15%">
        <div class="col">
            <h2 class="text-center">Delete Task</h2>
        </div>
    </div>

    <h3 class="col-md-offset-4">Are you sure you want to delete this?</h3>
    <div>
        <dl class="dl-horizontal col-md-offset-4">
            <dt>
                @Html.DisplayNameFor(model => model.User.Email)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.User.Email)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Description)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Description)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.DueDate)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.DueDate)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Complete)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Complete)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

        <div class="form-actions no-color col-md-offset-5">
            <input type="submit" value="Delete" class="btn btn-danger" />
            <a href="@Url.Action("Index", "UserTasks")" class="btn btn-primary">Back to List</a>
        </div>
        }
    </div>
</div>